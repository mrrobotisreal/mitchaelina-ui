# Dependencies
/node_modules

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# Env (identifiers, not secrets — but still local-only)
.env*.local
.env

# Misc
.DS_Store

# Electron desktop shell (self-contained package under desktop/). Build output
# and its own dependencies are local-only; the generated build/icon.png IS
# committed.
/desktop/dist/
/desktop/node_modules/
